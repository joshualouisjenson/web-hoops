export class GameOverScene extends Phaser.Scene {
  constructor() {
    super({ key: 'GameOverScene' });
  }

  preload(): void {
    // load sprite and background files
    // load sounds
  }

  create(): void {
    // render background, banner, and logo
    // show score and previous best
    // link out to coupon signup ??
    // show play again button
  }
}
