export class GameScene extends Phaser.Scene {
  constructor() {
    super({ key: 'GameScene' });
  }

  preload(): void {
    // load sprite and background files
    // load sounds
  }

  create(): void {
    // start timer
    // reset score
    // render hoop
    // initialize controls
  }
}
